name: Grand Bruxelles 19 Municipalities Accelerator

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/grand-bruxelles-19-municipalities-accelerator.yml"
      - "grand-bruxelles-game/tools/citygen/test_regional_acceleration_contract.py"
  schedule:
    - cron: "17 */2 * * *"
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/grand-bruxelles-19-municipalities-accelerator.yml"
      - "grand-bruxelles-game/tools/citygen/test_regional_acceleration_contract.py"
      - "grand-bruxelles-game/tools/citygen/build_brussels_regional_grid.py"
      - "grand-bruxelles-game/tools/citygen/autonomous_citygen.py"
      - ".github/workflows/grand-bruxelles-autonomous-citygen.yml"

permissions:
  contents: read
  actions: write

concurrency:
  group: grand-bruxelles-19-municipalities-accelerator
  cancel-in-progress: false

jobs:
  validate-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Validate 19-municipality acceleration contract
        run: python3 grand-bruxelles-game/tools/citygen/test_regional_acceleration_contract.py

  dispatch-regional-citygen:
    if: github.event_name != 'pull_request'
    needs: validate-contract
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Dispatch governed regional CityGen at maximum batch
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "/repos/${GITHUB_REPOSITORY}/actions/workflows/grand-bruxelles-autonomous-citygen.yml/dispatches" \
            -f ref=main \
            -f 'inputs[batch_size]=32'
          echo "REGIONAL_MUNICIPALITY_TARGET_OK municipalities=19 batch=32 cadence=2h immediate=true promotion_bypass=false"
