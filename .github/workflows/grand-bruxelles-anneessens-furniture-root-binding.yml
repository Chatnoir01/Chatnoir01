name: Grand Bruxelles Anneessens Furniture Root Binding

on:
  pull_request:
    paths:
      - ".github/workflows/grand-bruxelles-anneessens-furniture-root-binding.yml"
      - "grand-bruxelles-game/project.godot"
      - "grand-bruxelles-game/game/main.tscn"
      - "grand-bruxelles-game/data/osm/zones/anneessens/environment.game.json"
      - "grand-bruxelles-game/game/scripts/anneessens_osm_furniture_runtime.gd"
      - "grand-bruxelles-game/game/scripts/brussels_osm_tree_asset.gd"
      - "grand-bruxelles-game/game/tests/anneessens_osm_furniture_root_binding_test.gd"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  anneessens-furniture-root-binding:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Install display dependencies
        run: sudo apt-get update -qq && sudo apt-get install -y -qq xvfb libgl1-mesa-dri libglx-mesa0
      - name: Install Godot 4.7.1
        run: |
          curl -fL --retry 3 --retry-delay 3 -o /tmp/godot.zip https://github.com/godotengine/godot-builds/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.1-stable_linux.x86_64
      - name: Import project
        shell: bash
        run: |
          set -o pipefail
          /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --headless --path grand-bruxelles-game --editor --quit 2>&1 | tee /tmp/anneessens-furniture-root-import.log
          if grep -E "SCRIPT ERROR|Parse Error:|ERROR: Failed to load script" /tmp/anneessens-furniture-root-import.log; then exit 1; fi
      - name: Validate root-instantiated Anneessens furniture binding
        shell: bash
        run: |
          set -o pipefail
          /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --headless --path grand-bruxelles-game --script res://game/tests/anneessens_osm_furniture_root_binding_test.gd 2>&1 | tee /tmp/anneessens-furniture-root.log
          if grep -E "SCRIPT ERROR|Parse Error:|ANNEESSENS_OSM_FURNITURE_ROOT_BIND_FAIL" /tmp/anneessens-furniture-root.log; then exit 1; fi
          grep -q "ANNEESSENS_OSM_FURNITURE_ROOT_BIND_OK" /tmp/anneessens-furniture-root.log
