name: Grand Bruxelles Anneessens Roof Attachment

on:
  pull_request:
    paths:
      - ".github/workflows/grand-bruxelles-anneessens-roof-attachment.yml"
      - "grand-bruxelles-game/game/scripts/osm_city_builder.gd"
      - "grand-bruxelles-game/game/tests/anneessens_osm_roof_attachment_test.gd"
      - "grand-bruxelles-game/data/qa/playable_zone_catalog.json"
      - "grand-bruxelles-game/game/scripts/zone_selector_runtime.gd"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  roof-attachment:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - name: Install Godot 4.7.1
        run: |
          curl -fL --retry 3 --retry-delay 3 -o /tmp/godot.zip https://github.com/godotengine/godot-builds/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.1-stable_linux.x86_64
      - name: Import project
        run: /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --headless --path grand-bruxelles-game --editor --quit
      - name: Validate Anneessens OSM roof attachment and frozen A/B
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq xvfb libgl1-mesa-dri libglx-mesa0
          xvfb-run -a /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --path grand-bruxelles-game --display-driver x11 --rendering-method gl_compatibility --script res://game/tests/anneessens_osm_roof_attachment_test.gd
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: anneessens-roof-fix-ab
          path: grand-bruxelles-game/artifacts/qa/anneessens_roof_fix/*.png
