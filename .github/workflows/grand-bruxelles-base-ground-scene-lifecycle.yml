name: Grand Bruxelles Base Ground Scene Lifecycle

on:
  pull_request:
    paths:
      - 'grand-bruxelles-game/game/scripts/brussels_base_ground_surface_runtime.gd'
      - 'grand-bruxelles-game/tests/test_brussels_base_ground_scene_lifecycle.py'
      - '.github/workflows/grand-bruxelles-base-ground-scene-lifecycle.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lifecycle-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout exact candidate head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      - name: Verify exact PR head
        if: github.event_name == 'pull_request'
        shell: bash
        run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install pinned pytest
        run: python -m pip install --disable-pip-version-check 'pytest==8.3.5'
      - name: Run base-ground scene lifecycle regression
        working-directory: grand-bruxelles-game
        run: python -m pytest -q tests/test_brussels_base_ground_scene_lifecycle.py
