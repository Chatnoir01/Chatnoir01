name: Grand Bruxelles Grand-Place Architecture Arrival

on:
  pull_request:
    paths:
      - ".github/workflows/grand-bruxelles-grand-place-architecture-arrival.yml"
      - "grand-bruxelles-game/project.godot"
      - "grand-bruxelles-game/game/scripts/grand_place_official_surface.gd"
      - "grand-bruxelles-game/game/tests/grand_place_arrival_architecture_visual_test.gd"
      - "grand-bruxelles-game/data/urbis/grand_place_surface_42405.game.json"
      - "grand-bruxelles-game/data/osm/vertical_slice_01.game.json"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  architecture-arrival:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Install software renderer dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libgl1-mesa-dri libglx-mesa0 mesa-vulkan-drivers python3-pil
      - name: Install Godot 4.7.1
        run: |
          curl -fL --retry 3 --retry-delay 3 -o /tmp/godot.zip https://github.com/godotengine/godot-builds/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.1-stable_linux.x86_64
      - name: Import project
        run: /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --headless --path grand-bruxelles-game --editor --quit
      - name: Render deterministic architecture-bearing Grand-Place A/B
        shell: bash
        run: |
          set -o pipefail
          xvfb-run -a /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 \
            --path grand-bruxelles-game \
            --display-driver x11 \
            --rendering-method gl_compatibility \
            --resolution 1280x720 \
            --script res://game/tests/grand_place_arrival_architecture_visual_test.gd 2>&1 | tee /tmp/grand-place-architecture.log
          if grep -E "SCRIPT ERROR|Parse Error:|GRAND_PLACE_ARRIVAL_ARCH_FAIL" /tmp/grand-place-architecture.log; then exit 1; fi
          grep -F "GRAND_PLACE_ARRIVAL_ARCH_OK" /tmp/grand-place-architecture.log
          test -f grand-bruxelles-game/artifacts/grand-place/arrival-architecture/before.png
          test -f grand-bruxelles-game/artifacts/grand-place/arrival-architecture/after.png
      - name: Measure player-facing delta
        run: |
          python3 - <<'PY'
          from PIL import Image
          from pathlib import Path
          before = Image.open('grand-bruxelles-game/artifacts/grand-place/arrival-architecture/before.png').convert('RGB')
          after = Image.open('grand-bruxelles-game/artifacts/grand-place/arrival-architecture/after.png').convert('RGB')
          assert before.size == after.size == (1280, 720)
          b = before.load(); a = after.load()
          strong = 0
          minx=miny=10**9; maxx=maxy=-1
          for y in range(720):
              for x in range(1280):
                  d = max(abs(a[x,y][i]-b[x,y][i]) for i in range(3))
                  if d > 3:
                      strong += 1
                      minx=min(minx,x); maxx=max(maxx,x); miny=min(miny,y); maxy=max(maxy,y)
          pct = strong / (1280*720) * 100.0
          bbox = None if strong == 0 else (minx,miny,maxx,maxy)
          print(f'GRAND_PLACE_ARCH_A_B: strong={strong} strong_pct={pct:.3f}% bbox={bbox}')
          Path('/tmp/grand-place-architecture-ab.txt').write_text(f'strong_pixels={strong}\nstrong_pct={pct:.6f}\nbbox={bbox}\n')
          assert strong > 15000, 'anti-micro gate: Grand-Place correction must be obvious at normal view'
          PY
      - name: Upload visual evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: grand-place-architecture-arrival-evidence
          path: |
            grand-bruxelles-game/artifacts/grand-place/arrival-architecture/before.png
            grand-bruxelles-game/artifacts/grand-place/arrival-architecture/after.png
            /tmp/grand-place-architecture.log
            /tmp/grand-place-architecture-ab.txt
          if-no-files-found: warn
