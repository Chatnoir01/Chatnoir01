name: Grand Bruxelles Historic Terrace Awning Visual

on:
  pull_request:
    paths:
      - ".github/workflows/grand-bruxelles-historic-terrace-awning-visual.yml"
      - "grand-bruxelles-game/game/scripts/brussels_historic_terrace_awning.gd"
      - "grand-bruxelles-game/game/tests/brussels_historic_terrace_awning_test.gd"
      - "grand-bruxelles-game/game/tests/brussels_historic_terrace_awning_witness_test.gd"
      - "grand-bruxelles-game/data/provenance/brussels_historic_terrace_awning.json"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historic-terrace-awning:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Install software renderer dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libgl1-mesa-dri libglx-mesa0 mesa-vulkan-drivers
      - name: Install Godot 4.7.1
        run: |
          curl -fL --retry 3 --retry-delay 3 -o /tmp/godot.zip https://github.com/godotengine/godot-builds/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.1-stable_linux.x86_64
      - name: Import project
        run: /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --headless --path grand-bruxelles-game --editor --quit
      - name: Validate reusable historic awning
        shell: bash
        run: |
          set -o pipefail
          xvfb-run -a /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --path grand-bruxelles-game --display-driver x11 --rendering-method gl_compatibility --script res://game/tests/brussels_historic_terrace_awning_test.gd 2>&1 | tee /tmp/historic-awning.log
          if grep -E "SCRIPT ERROR|Parse Error:|BRUSSELS_HISTORIC_TERRACE_AWNING_FAIL" /tmp/historic-awning.log; then exit 1; fi
          grep -F "BRUSSELS_HISTORIC_TERRACE_AWNING_OK" /tmp/historic-awning.log
      - name: Capture production witness
        shell: bash
        run: |
          set -o pipefail
          xvfb-run -a /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --path grand-bruxelles-game --display-driver x11 --rendering-method gl_compatibility --resolution 1280x720 --script res://game/tests/brussels_historic_terrace_awning_witness_test.gd 2>&1 | tee /tmp/historic-awning-witness.log
          if grep -E "SCRIPT ERROR|Parse Error:|BRUSSELS_HISTORIC_TERRACE_AWNING_WITNESS_FAIL" /tmp/historic-awning-witness.log; then exit 1; fi
          grep -F "BRUSSELS_HISTORIC_TERRACE_AWNING_WITNESS_OK" /tmp/historic-awning-witness.log
          test -f grand-bruxelles-game/artifacts/visual/brussels_historic_terrace_awning_witness.png
      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: historic-terrace-awning-visual-evidence
          path: |
            grand-bruxelles-game/artifacts/visual/brussels_historic_terrace_awning_witness.png
            /tmp/historic-awning.log
            /tmp/historic-awning-witness.log
          if-no-files-found: error
