name: Grand Bruxelles Intake Driveway Access
on:
  push:
    branches: ["intake/brussels-driveway-access-current"]
    paths: [".github/workflows/grand-bruxelles-intake-driveway-access.yml"]
  workflow_dispatch:
permissions: {contents: read}
jobs:
  download:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Download official Brussels driveway access inventory
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/driveway-access
          python3 - <<'PY'
          import hashlib,json,urllib.parse,urllib.request
          base='https://data.mobility.brussels/geoserver/ows'; layer='bm_parking:access_carrossables'; headers={'User-Agent':'GrandBruxellesGame-DataFactory/1.0'}
          url=base+'?'+urllib.parse.urlencode({'service':'WFS','version':'1.1.0','request':'GetFeature','typeName':layer,'outputFormat':'json','srsName':'EPSG:31370'})
          with urllib.request.urlopen(urllib.request.Request(url,headers=headers),timeout=180) as r: raw=r.read()
          obj=json.loads(raw); count=len(obj.get('features',[])); assert count>0
          open('/tmp/driveway-access/driveway_access_epsg31370.geojson','wb').write(raw)
          open('/tmp/driveway-access/manifest.json','w').write(json.dumps({'publisher':'Bruxelles Mobilite / Parking.brussels','dataset':'Acces carrossables','layer':layer,'license':'CC0','source_url':url,'crs':'EPSG:31370','feature_count':count,'bytes':len(raw),'sha256':hashlib.sha256(raw).hexdigest(),'runtime_authorized':False},indent=2,ensure_ascii=False))
          print('DRIVEWAY_ACCESS_INTAKE_OK',count)
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: grand-bruxelles-driveway-access
          path: /tmp/driveway-access
          if-no-files-found: error
          retention-days: 14
          compression-level: 6
