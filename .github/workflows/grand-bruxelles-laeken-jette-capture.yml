name: Grand Bruxelles Laeken Jette Capture

on:
  push:
    branches: ["zone-laeken-jette"]
    paths:
      - "grand-bruxelles-game/game/zones/laeken_jette/**"
      - "grand-bruxelles-game/game/tests/render_laeken_jette_capture.gd"
      - ".github/workflows/grand-bruxelles-laeken-jette-capture.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout zone branch
        uses: actions/checkout@v4
        with:
          ref: zone-laeken-jette

      - name: Install runtime dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends xvfb libgl1-mesa-dri libglx-mesa0

      - name: Install Godot 4.7.1
        run: |
          curl -fL --retry 3 --retry-delay 2 -o /tmp/godot.zip \
            https://github.com/godotengine/godot-builds/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.1-stable_linux.x86_64

      - name: Render Laeken Jette preview
        shell: bash
        run: |
          set -o pipefail
          xvfb-run -a -s "-screen 0 1280x720x24" \
            /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 \
            --path grand-bruxelles-game \
            --rendering-method gl_compatibility \
            --script res://game/tests/render_laeken_jette_capture.gd 2>&1 | tee /tmp/capture.log
          if grep -E "SCRIPT ERROR|Parse Error:|CAPTURE_FAIL" /tmp/capture.log; then
            exit 1
          fi
          grep -q "CAPTURE_OK" /tmp/capture.log
          test -s grand-bruxelles-game/laeken_jette_preview.png

      - name: Upload preview image
        uses: actions/upload-artifact@v4
        with:
          name: laeken-jette-preview
          path: grand-bruxelles-game/laeken_jette_preview.png
          if-no-files-found: error
          retention-days: 7
