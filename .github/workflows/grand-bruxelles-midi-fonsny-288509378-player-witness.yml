name: Grand Bruxelles Midi Fonsny 288509378 Player Witness

on:
  pull_request:
    paths:
      - ".github/workflows/grand-bruxelles-midi-fonsny-288509378-player-witness.yml"
      - "grand-bruxelles-game/game/tests/midi_fonsny_288509378_player_witness_test.gd"
      - "grand-bruxelles-game/game/scripts/automatic_road_direct_spawn.gd"
      - "grand-bruxelles-game/game/scripts/generic_osm_surface_collision_runtime.gd"
      - "grand-bruxelles-game/game/scripts/osm_midi_mask.gd"
      - "grand-bruxelles-game/game/main.tscn"
      - "grand-bruxelles-game/data/osm/vertical_slice_01.game.json"
      - "grand-bruxelles-game/data/runtime/road_destination_runtime_index.json"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fonsny-288509378-player-witness:
    runs-on: ubuntu-24.04
    timeout-minutes: 18
    env:
      GODOT_SHA256: c7ff14fd28472c8d4f193043de30278dcf7e5241a1dcf7566b02e27addaa33ba
      EXPECTED_SOURCE_SHA256: 899bc73ee0eea3623d7cc45455a542c1704039ef0239c13c33b3c74b4a241398
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Require exact-current-main ancestry and exact head
        shell: bash
        run: |
          set -euo pipefail
          HEAD_SHA="$(git rev-parse HEAD)"
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            test "$HEAD_SHA" = "${{ github.event.pull_request.head.sha }}"
          fi
          git fetch origin main --no-tags
          MAIN_SHA="$(git rev-parse origin/main)"
          test "$(git merge-base "$HEAD_SHA" "$MAIN_SHA")" = "$MAIN_SHA"
          printf 'WITNESS_BASE_SHA=%s\n' "$MAIN_SHA" >> "$GITHUB_ENV"
          printf 'WITNESS_HEAD_SHA=%s\n' "$HEAD_SHA" >> "$GITHUB_ENV"
          echo "$EXPECTED_SOURCE_SHA256  grand-bruxelles-game/data/osm/vertical_slice_01.game.json" | sha256sum -c -

      - name: Install verified Godot 4.7.1 and Xvfb
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends xvfb libgl1 libx11-6 libxcursor1 libxinerama1 libxi6 libxrandr2 libasound2t64
          curl -fL --retry 5 --retry-all-errors --retry-delay 3 -o /tmp/godot.zip "https://github.com/godotengine/godot-builds/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip"
          echo "$GODOT_SHA256  /tmp/godot.zip" | sha256sum -c -
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.1-stable_linux.x86_64
          /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --version | grep -q '^4\.7\.1\.stable'

      - name: Import exact-head project
        shell: bash
        run: |
          set -euo pipefail
          /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --headless --path grand-bruxelles-game --editor --quit 2>&1 | tee /tmp/import.log
          ! grep -E "SCRIPT ERROR|Parse Error:|ERROR: Failed to load script" /tmp/import.log

      - name: Capture exact source-backed road-288509378 player witness
        shell: bash
        run: |
          set -euo pipefail
          timeout 180s xvfb-run -a /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --path grand-bruxelles-game --script res://game/tests/midi_fonsny_288509378_player_witness_test.gd 2>&1 | tee /tmp/witness.log
          ! grep -E "SCRIPT ERROR|Parse Error:|MIDI_FONSNY_288509378_PLAYER_WITNESS_FAIL" /tmp/witness.log
          test "$(grep -c 'MIDI_FONSNY_288509378_PLAYER_WITNESS_OK' /tmp/witness.log)" -eq 1

      - name: Seal fail-closed player witness receipt
        shell: bash
        run: |
          set -euo pipefail
          DIR=grand-bruxelles-game/artifacts/qa/midi_fonsny_288509378_player_witness
          RECEIPT="$DIR/receipt.json"
          PNG="$DIR/road-288509378.png"
          test -s "$RECEIPT"
          test -s "$PNG"
          python3 - "$RECEIPT" "$PNG" "$WITNESS_BASE_SHA" "$WITNESS_HEAD_SHA" "$EXPECTED_SOURCE_SHA256" <<'PY'
          import hashlib,json,os,struct,sys
          receipt,png,base,head,expected_source_sha=sys.argv[1:]
          d=json.load(open(receipt,encoding='utf-8'))
          assert d['schema']=='grand-bruxelles-midi-fonsny-288509378-player-witness-v1'
          assert d['road_osm_id']==288509378 and d['request']=='road-288509378'
          assert d['resolution']==[1280,720]
          assert d['source_path']=='res://data/osm/vertical_slice_01.game.json'
          assert d['source_sha256']==expected_source_sha
          assert d['source_name']=='Avenue Fonsny - Fonsnylaan'
          assert d['lookup_mode']=='deterministic_runtime_index'
          assert d['source_sightline_clear'] is True
          assert d['resolver_camera_local_transform_preserved'] is True
          assert d['resolver_camera_fov_preserved'] is True
          assert d['runtime_camera_local_transform_may_evolve'] is True
          assert d['requested_osm_identity_preserved'] is True
          assert d['same_way_arc_runtime_required'] is True
          assert d['same_requested_osm_way_only'] is True
          assert int(d['source_arc_segment_hops']) >= 1
          assert 0.0 < float(d['axis_lookahead_m']) <= 22.0
          assert float(d['axis_alignment']) >= 0.90
          assert d['cross_way_traversal_used'] is False
          assert d['source_geometry_changed'] is False
          assert d['collision_geometry_changed'] is False
          assert d['resolver_thresholds_lowered'] is False
          assert d['osm_to_urbis_crosswalk_claimed'] is False
          assert d['human_visual_review_required'] is True
          assert d['visual_acceptance'] is False
          assert d['destination_advertisable'] is False
          assert d['jouable_authorized'] is False
          with open(png,'rb') as f:
              sig=f.read(8); length=struct.unpack('>I',f.read(4))[0]; kind=f.read(4); ihdr=f.read(length)
          assert sig==b'\x89PNG\r\n\x1a\n' and kind==b'IHDR'
          width,height=struct.unpack('>II',ihdr[:8])
          assert (width,height)==(1280,720)
          assert os.path.getsize(png) > 50000
          d['base_sha']=base
          d['head_sha']=head
          d['png_sha256']=hashlib.sha256(open(png,'rb').read()).hexdigest()
          open(receipt,'w',encoding='utf-8').write(json.dumps(d,indent=2,sort_keys=True)+'\n')
          PY
          sha256sum "$PNG" "$RECEIPT" | tee /tmp/witness.sha256

      - name: Upload exact-head player witness
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: grand-bruxelles-midi-fonsny-288509378-player-witness
          path: |
            grand-bruxelles-game/artifacts/qa/midi_fonsny_288509378_player_witness/
            /tmp/witness.log
            /tmp/import.log
            /tmp/witness.sha256
          if-no-files-found: warn
          retention-days: 7
