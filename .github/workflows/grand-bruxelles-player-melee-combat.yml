name: Grand Bruxelles Player Melee Combat

on:
  pull_request:
    paths:
      - ".github/workflows/grand-bruxelles-player-melee-combat.yml"
      - "grand-bruxelles-game/project.godot"
      - "grand-bruxelles-game/game/scripts/player_melee_combat_runtime.gd"
      - "grand-bruxelles-game/game/scripts/npc_agent.gd"
      - "grand-bruxelles-game/game/tests/player_melee_combat_test.gd"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  player-melee-combat:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Install Godot 4.7.1
        run: |
          curl -fL --retry 3 --retry-delay 3 -o /tmp/godot.zip https://github.com/godotengine/godot-builds/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.1-stable_linux.x86_64
      - name: Import project
        shell: bash
        run: |
          set -o pipefail
          /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --headless --path grand-bruxelles-game --editor --quit 2>&1 | tee /tmp/player-melee-import.log
          if grep -E "SCRIPT ERROR|Parse Error:|ERROR: Failed to load script" /tmp/player-melee-import.log; then exit 1; fi
      - name: Validate melee player verb
        shell: bash
        run: |
          set -o pipefail
          /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --headless --path grand-bruxelles-game --script res://game/tests/player_melee_combat_test.gd 2>&1 | tee /tmp/player-melee.log
          if grep -E "SCRIPT ERROR|Parse Error:|PLAYER_MELEE_COMBAT_FAIL" /tmp/player-melee.log; then exit 1; fi
          grep -F "PLAYER_MELEE_COMBAT_OK" /tmp/player-melee.log
