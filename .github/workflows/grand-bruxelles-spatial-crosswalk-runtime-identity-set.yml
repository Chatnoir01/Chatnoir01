name: Grand Bruxelles Spatial Crosswalk Runtime Identity Set

on:
  pull_request:
    paths:
      - ".github/workflows/grand-bruxelles-spatial-crosswalk-runtime-identity-set.yml"
      - "grand-bruxelles-game/data/runtime/road_destination_runtime_index.json"
      - "grand-bruxelles-game/data/osm/vertical_slice_01.game.json"
      - "grand-bruxelles-game/tools/city_machine/validate_spatial_crosswalk_runtime_identity_set.py"
      - "grand-bruxelles-game/tests/test_spatial_crosswalk_runtime_identity_set.py"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runtime-identity-set:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
      - name: Require exact-current-main base and exact head
        shell: bash
        run: |
          set -euo pipefail
          HEAD_SHA="$(git rev-parse HEAD)"
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            PR_HEAD_SHA="${{ github.event.pull_request.head.sha }}"
            PR_BASE_SHA="${{ github.event.pull_request.base.sha }}"
            test "$HEAD_SHA" = "$PR_HEAD_SHA"
          else
            PR_BASE_SHA="$(git merge-base HEAD origin/main)"
          fi
          git fetch origin main --no-tags
          MAIN_SHA="$(git rev-parse origin/main)"
          test "$MAIN_SHA" = "$PR_BASE_SHA"
          test "$(git merge-base "$HEAD_SHA" "$MAIN_SHA")" = "$MAIN_SHA"
      - name: Validate immutable runtime road identity set
        working-directory: grand-bruxelles-game
        run: |
          set -euo pipefail
          python3 tools/city_machine/validate_spatial_crosswalk_runtime_identity_set.py
          python3 tests/test_spatial_crosswalk_runtime_identity_set.py
