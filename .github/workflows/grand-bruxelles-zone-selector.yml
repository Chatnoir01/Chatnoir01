name: Grand Bruxelles Zone Selector

on:
  pull_request:
    paths:
      - ".github/workflows/grand-bruxelles-zone-selector.yml"
      - "grand-bruxelles-game/data/qa/playable_zone_catalog.json"
      - "grand-bruxelles-game/tools/validate_human_zone_promotion.py"
      - "grand-bruxelles-game/game/scripts/zone_selector_runtime.gd"
      - "grand-bruxelles-game/game/scripts/player_controller.gd"
      - "grand-bruxelles-game/game/tests/zone_selector_runtime_test.gd"
      - "grand-bruxelles-game/project.godot"
      - "grand-bruxelles-game/docs/VISUAL_MASTERLIST.md"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  zone-selector:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Prove human-only LABO promotion gate
        shell: bash
        run: |
          python grand-bruxelles-game/tools/validate_human_zone_promotion.py --self-test
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git show "${{ github.event.pull_request.base.sha }}:grand-bruxelles-game/data/qa/playable_zone_catalog.json" > /tmp/base_playable_zone_catalog.json
            python grand-bruxelles-game/tools/validate_human_zone_promotion.py \
              --base /tmp/base_playable_zone_catalog.json \
              --head grand-bruxelles-game/data/qa/playable_zone_catalog.json
          fi
      - name: Install Godot 4.7.1
        run: |
          curl -fL --retry 3 --retry-delay 3 -o /tmp/godot.zip https://github.com/godotengine/godot-builds/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.1-stable_linux.x86_64
      - name: Import project
        run: /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --headless --path grand-bruxelles-game --editor --quit
      - name: Validate listed zones and selector runtime
        run: /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --headless --path grand-bruxelles-game --script res://game/tests/zone_selector_runtime_test.gd
      - name: Capture zone menu over production main
        run: sudo apt-get update -qq && sudo apt-get install -y -qq xvfb libgl1-mesa-dri libglx-mesa0 && xvfb-run -a /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 --path grand-bruxelles-game --display-driver x11 --rendering-method gl_compatibility --script res://game/tests/zone_selector_runtime_test.gd -- capture=1
      - uses: actions/upload-artifact@v4
        with:
          name: zone-selector-witness
          path: grand-bruxelles-game/artifacts/visual/zone_selector_1280x720.png
