# City Machine finish pipeline pilot

1. Midi is JOUABLE, but current main does not prove a zone-scoped machine rebuild from Midi buildings + streets inputs.
2. Jette has committed UrbIS phase2 buildings + street surfaces and an existing one-command deterministic rebuild.
3. Therefore the campaign pilot is **Jette** under the fixed selection law.
4. The pilot is locked for this campaign; no mid-run zone switch is allowed.
5. Missing optional layers log SKIP/disabled and never invent source geometry.

Rebuild from the repository root with `python3 grand-bruxelles-game/tools/city_machine/city_machine.py build --zone jette` after installing `pyproj>=3.7,<4`. The five-family status is machine-readable in `tools/city_machine/finish_registry.json`. Geometry then OSM environment then finish then life then proof is the only allowed order. A hard gate failure must stop with a non-zero exit code. Human LABO→JOUABLE promotion remains outside automation.
