{
  "name": "pilot-belgium",
  "version": "0.3.0",
  "private": true,
  "description": "Pilot — gestion commerciale pour indépendants belges",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "db:migrate": "node scripts/migrate-postgres.mjs"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=24"
  }
}
